const { Client, MESSAGES } = require('./i3wm');

Client.connect().then(async client => {
  const res = await client.subscribe('tick');

  client.on('window', (msg) => {
    console.log('tick', msg)
  })

  client.connection.destroy()

  //await client.message('run_command', 'workspace 0; mark m')
});

