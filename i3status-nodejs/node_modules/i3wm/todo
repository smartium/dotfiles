- on 'data' event can contain many messages, rearrange event emitting in such a away to handle multiple message per tick

- deconstruct array reply for run_command when using `command` helper

- throw error if no success